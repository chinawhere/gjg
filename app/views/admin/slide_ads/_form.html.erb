<%= simple_form_for [:admin, @slide_ad] do |f| %>
	<%= f.input :title, label: false, placeholder: '输入标题文字' %><br>
	<%= f.input :link, label: false, placeholder: '输入图片链接' %><br>
	<%= f.input :img, label: false %><br>
	<%= f.submit '提交', class:'btn btn-primary'%>
<% end %>
