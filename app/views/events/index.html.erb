<% @events.each do |event| %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title"><%= link_to event.name, event_path(event) %></h3>
    </div>
    <div class="panel-body">
      <%= event.content.try(:html_safe) %>
    </div>
  </div>
<% end -%>